/**
 * Shared constants used across the plugin.
 */
export const PLUGIN_NAME = "Logseq Task to Calendar";

/**
 * Settings key used by Logseq to persist plugin preferences.
 */
export const PLUGIN_SETTINGS_KEY = "logseq-task-to-calendar";

/**
 * Property stored on Logseq blocks to remember the remote calendar event id.
 */
export const REMOTE_EVENT_ID_PROPERTY = "cal-event-id";
