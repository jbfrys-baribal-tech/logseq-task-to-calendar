/**
 * Supported calendar providers for the MVP and future extensions.
 */
export type CalendarProviderType = "google" | "outlook";

/**
 * Available behaviors when a synchronized task is marked as completed.
 */
export type CompletedTaskAction = "update" | "delete";
